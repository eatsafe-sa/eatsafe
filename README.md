# EatSafe

## Dev Setup

Create a file named `.env.local.php` with this data, updated with your info:

```
<?php

return array(

	'DB_HOST' => 'localhost',
	'DB_NAME' => 'eatsafe',
	'DB_USER' => 'codeup',
	'DB_PASS' => 'password',

);
```